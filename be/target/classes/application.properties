spring.application.name=crypto-mock-trading

# Binance WebSocket Configuration
binance.websocket.base-url=wss://data-stream.binance.vision
binance.websocket.stream-url=wss://data-stream.binance.vision/stream
binance.websocket.reconnect-delay=5000
binance.websocket.max-reconnect-attempts=10
binance.websocket.ping-interval=180000

# Binance Stream Configuration
binance.streams.enabled=true
binance.streams.symbols=btcusdt,ethusdt,bnbusdt,adausdt,solusdt
binance.streams.kline-interval=1m
binance.streams.depth-level=5

# Redis Configuration (for caching market data)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=60000

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/mock_trading
spring.datasource.username=postgres
spring.datasource.password=phonghoang68
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# Logging
logging.level.com.example.demo.client.binance=DEBUG
logging.level.org.springframework.web.socket=DEBUG
